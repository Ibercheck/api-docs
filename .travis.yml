language: ruby
os:
  - linux
dist: xenial

rvm:
  - 2.5

cache: bundler

script:
  - bundle exec middleman build

deploy:
  provider: pages
  local_dir: build/
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  target_branch: gh-pages
  on:
    branch: master
